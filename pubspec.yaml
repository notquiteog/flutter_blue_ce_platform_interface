name: flutter_blue_ce_platform_interface
description: A common platform interface for the flutter_blue_ce plugin.
version: 2.3.8+ce.1
publish_to: none
homepage: https://github.com/notquiteog/flutter_blue_ce_platform_interface

environment:
  sdk: ^3.0.0
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
